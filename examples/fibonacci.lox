// Fibonacci sequence calculator
// Demonstrates recursion and functions

fun fib(n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);
}

var before = clock();
print fib(40)
var after clock();

print after - before

